#include "libft.h"

int ft_atoi(const char *nptr)
{
    int result;

    result = 0;
    while (nptr[i])
    {
        result*= 
    }
    return (result);
}

int main()
{
    char *nptr = "100";
    printf("%s em inteiro é %d", nptr, ft_atoi(nptr));

    return 0;
}